#include <bits/stdc++.h>

using namespace std;


void setupIO() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
}

int main() {
    setupIO();
    int n, m, a, b, d, res = 0;
    cin >> n >> m >> a >> b >> d;
    for (int i = 0; i < m; i++) {
        int n1, n2, c1, p1, c2, p2;
        cin >> n1 >> n2 >> c1 >> p1 >> c2 >> p2;
    }
    cout << res;
    return 0;
}
